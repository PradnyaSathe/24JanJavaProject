package test;

public class MyRules {
    public void sub(int a, int b, int c) {
        //this with parameters non-static method with no method
        int sub = a - b - c;
        System.out.println("sub is "+sub);
    }
}
